{
  "buildCommand": "npm run build --workspace=packages/shared-types && npm run build --workspace=apps/frontend",
  "outputDirectory": "apps/frontend/.next",
  "installCommand": "npm install --ignore-optional",
  "framework": "nextjs"
}
